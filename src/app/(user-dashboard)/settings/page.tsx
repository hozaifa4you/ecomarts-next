import { AccountSettings } from "@/components/Dashboard/AccountSettings";

const SettingsPage = () => {
   return (
      <div className="show fade active">
         <AccountSettings />
      </div>
   );
};

export default SettingsPage;
